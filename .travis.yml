language: ruby
rvm:
  - ruby-1.9.3-p362
env:
  - DB=mongodb
service:
	- mongodb
script:
  - RAILS_ENV=test bundle exec rake db:create
  - RAILS_ENV=test bundle exec rake db:migrate
  - bundle exec rake
before_script:
	- mongo cast_test --eval 'db.addUser("travis", "test");'