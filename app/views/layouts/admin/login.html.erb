<!DOCTYPE html>
<html lang="en">
<head>
  <title>Cast Zone</title>
  <%= stylesheet_link_tag    "admin", :media => "all" %>
  <%= javascript_include_tag "admin" %>
  <%= csrf_meta_tags %>
</head>
  
  <body id="login">
  
    <div id="login_container">
      <div id="login_form">
        <br>
        <%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
          <p><%= f.email_field :email, autofocus: true, placeholder: "Email", class: "{validate: {required: true}}" %></p>
          <p><%= f.password_field :password, placeholder: "Password", class: "{validate: {required: true}}" %></p>
          <button type="submit" class="button blue">Login</button>
        <% end %>
      </div>
    </div>
  </body>
</html>